# User-scope Claude Code config

Everything here is inherited by every workspace on this machine. A skill lands here when
it has been useful in two workspaces; until then it stays workspace-scoped.

| Folder | Contents |
|:---|:---|
| `skills/` | Three cross-project skills |
| `commands/` | Two slash commands |
| `agents/` | One sub-agent |
| `output-styles/` | One response style |
