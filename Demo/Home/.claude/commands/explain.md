---
description: Explain how a piece of code actually works, tracing the real path rather than the intended one.
---

Trace the actual execution path for the named entry point. Follow the calls, not the
comments — where the two disagree, say so, because that disagreement is usually the
interesting part.

End with the one thing a newcomer would get wrong about this code.
