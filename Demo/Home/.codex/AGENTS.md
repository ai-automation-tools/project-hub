# Codex — user scope

Applies everywhere on this machine unless a workspace `AGENTS.md` overrides it.

- Read before writing. Trace the real call path, not the one the comments describe.
- Smallest change that works. An abstraction with one caller is not an abstraction.
- Run the repo's own checks before reporting done. Never report done on a partial run.
