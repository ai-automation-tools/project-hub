# Codex — Example Workspace

Same ground rules as `Agents/Claude/CLAUDE.md`. This runtime is used for schema work
specifically, because the diff review is better at SQL than at prose.

Run `npm test` in whatever repo you touched before reporting done.
