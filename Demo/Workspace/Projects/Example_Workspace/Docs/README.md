# Docs

Workspace-level documentation — the things that outlive any one repo.

Architecture decisions live in `decisions/`, numbered and never edited after they land.
Anything describing how a single repo works belongs in that repo instead.
