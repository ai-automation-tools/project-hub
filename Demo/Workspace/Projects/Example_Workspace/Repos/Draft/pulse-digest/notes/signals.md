# Which signal

Downloads look like the obvious metric and are probably wrong.

A format downloaded once per export is being used. A format downloaded five times per
export is being fought with — somebody is re-exporting because the first one was not
what they needed. Those two look identical in a raw count and mean opposite things.

Next step: pull 90 days and split by distinct user per job, not by raw event count.
