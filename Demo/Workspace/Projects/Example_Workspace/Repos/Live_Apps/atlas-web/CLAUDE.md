# atlas-web

Presentation only. Business logic goes to `signal-api`.

- Components come from `studio-kit`. Do not fork one into this repo to change a colour —
  change the token, or add a variant upstream.
- No new state library. The two that are here already cover everything so far.
- Run `npm test && npm run lint` before reporting done.
