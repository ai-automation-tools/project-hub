# data-lens docs

One page, because it is one command with four subcommands.

`summary` counts rows and nulls per column. `columns` prints inferred types. `head` and
`sample` do what they sound like — `sample` is reservoir sampling, so it is a fair sample
of a file it never fully reads.
