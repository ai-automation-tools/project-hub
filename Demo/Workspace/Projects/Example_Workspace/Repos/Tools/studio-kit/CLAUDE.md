# studio-kit

Tokens first. If a change adds a hex value to a component, it is the wrong change.

Run `npm test && npm run build` before reporting done — the build catches token
references that the tests do not.
