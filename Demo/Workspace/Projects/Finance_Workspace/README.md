# Finance Workspace

Two small services that move numbers between systems nobody here controls. Flat repo
layout — no group tier, because two repos do not need one.

Everything in this workspace is reconciliation. If a change cannot be checked against a
second source, it is not finished.
