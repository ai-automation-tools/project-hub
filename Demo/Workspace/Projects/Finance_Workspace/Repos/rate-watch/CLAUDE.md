# rate-watch

Store every response, including the ones that look wrong. Never pick a winner.

The threshold is config. Do not inline it, and do not change the default without a
reason written down somewhere durable.
